{{> layout/header}}

<div class="update-container">
    <div class="update-container-content">
        <div class="update-card">
            <!-- 헤더 -->
            <div class="update-header">
                <i class="fa-solid fa-calendar-check"></i>
                <span>근무 스케줄 수정</span>
            </div>

            <!-- 폼 -->
            <form action="/schedule/{{schedule.id}}/update" method="post">
                <div class="update-body">
                    <!-- 직원 ID 숨김 -->
                    <input type="hidden" name="employeeId" value="{{schedule.id}}">

                    <!-- 이름 (readonly) -->
                    <div class="update-item">
                        <span class="update-label">이름:</span>
                        <span class="update-value">{{schedule.name}}</span>
                    </div>

                    <!-- 시작 시간 -->
                    <div class="update-item">
                        <label class="update-label" for="startTime">시작 시간:</label>
                        <input type="time" id="startTime" name="startTime" required>
                    </div>

                    <!-- 종료 시간 -->
                    <div class="update-item">
                        <label class="update-label" for="endTime">종료 시간:</label>
                        <input type="time" id="endTime" name="endTime" required>
                    </div>
                </div>

                <div class="bottom-buttons">
                    <button type="submit" class="btn btn-add">수정</button>
                    <a href="/schedule/list" class="btn stock-badge badge-warn">취소</a>
                </div>
            </form>
        </div>
    </div>
</div>

{{> layout/footer}}