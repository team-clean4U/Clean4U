{{> layout/header}}

{{> layout/background}}

<!-- http://localhost:8080/laundry-item/1 -->
<div class="detail-container">
    <div class="detail-container-content">
        <div class="buttons">
            <form action="/laundry-item/list" method="get">
                <button class="btn btn-back"><i class="fa-solid fa-arrow-left"></i>목록으로</button>
            </form>
            <div class="button-right">
                <a href="/laundry-item/{{laundryItem.id}}/update" class="btn btn-edit">
                    <i class="fa-solid fa-edit"></i> 수정
                </a>
                <form action="/laundry-item/{{laundryItem.id}}/delete" method="post">
                    <button class="btn btn-delete"><i class="fa-solid fa-trash"></i>삭제</button>
                </form>
            </div>
        </div>
        <div class="detail-card">
            <div class="detail-header">
                <i class="fa-solid fa-{{laundryItem.icon}}"></i>
                <h4>{{laundryItem.name}}</h4>
            </div>
            <div class="detail-body">
                <div class="detail-item">
                    <span class="detail-label">
                        <i class="fa-solid fa-list"></i>
                        카테고리
                    </span>
                    <span class="detail-value category">
                        {{laundryItem.category}}
                    </span>
                </div>
                <div class="detail-item">
                    <span class="detail-label">
                        <i class="fa-solid fa-won-sign"></i>
                        기본 가격
                    </span>
                    <span class="detail-value price">
                        {{laundryItem.basePrice}}
                    </span>
                </div>
                <div class="detail-item">
                    <span class="detail-label">
                        <i class="fa-solid fa-circle-info"></i>
                        설명
                    </span>
                    <span class="detail-value">
                        {{laundryItem.description}}
                    </span>
                </div>
                <div class="detail-item">
                    <span class="detail-label">
                        <i class="fa-solid fa-hourglass"></i>
                        생성일
                    </span>
                    <span class="detail-value">
                        {{laundryItem.createdAt}}
                    </span>
                </div>
            </div>
        </div>
    </div>
</div>

{{> layout/footer}}